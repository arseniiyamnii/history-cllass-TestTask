# history-cllass-TestTask
## Пояснение некоторых деталей:
 Я никогда не работал с jupyter.
 Его я запускал через venv.
 Так что не уверен как у вас это будет работать.
 Также в коде указаны лимиты размера history_arr в 3 и 5 Мб. 
 На моем компьютере я бы долго обрабатывал , если бы были 3 и 5 Гб.
 Меняются эти переменные в main.py
 Соответственно:
 `first_stop_at_MB` и `second_stop_at_MB`
## For run test:
  `pip install -r requirements.txt`

  `python3 main.py`
## For use lib:
  `pip install yamniiMod`

